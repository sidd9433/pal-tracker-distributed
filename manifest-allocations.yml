applications:
- name: tracker-allocations
  path: ./applications/allocations-server/build/libs/allocations-server.jar
  routes:
  - route: allocations-pal-sid-mukherjee-${UNIQUE_IDENTIFIER}.${DOMAIN}
  memory: 1G
  instances: 1
  env:
    REGISTRATION_SERVER_ENDPOINT: registration-pal-sid-mukherjee-${UNIQUE_IDENTIFIER}.${DOMAIN}
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'
  services:
  - tracker-allocations-database
